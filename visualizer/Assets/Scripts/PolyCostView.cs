using System;
using UnityEngine;


namespace PowerNetwork.View
{
    public class PolyCostView : MonoBehaviour
    {
        public PolyCost polyCost;

       /* public GeneratorView elGenerator;
    

        public Action<PolyCost> OnPolyCostChanged;
        private void Awake()
        {
            polyCost.element = elGenerator.GeneratorData.bus;         
        }

        public void NotifyChange()
        {
            OnPolyCostChanged?.Invoke(polyCost);
        }*/
    }
}

